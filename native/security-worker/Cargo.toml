[package]
name = "deltamod-security-worker"
version = "0.1.0"
edition = "2021"
license = "EUPL-1.2"

[dependencies]
deltamod-native-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
