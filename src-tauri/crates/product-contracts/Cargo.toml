[package]
name = "deltamod-product-contracts"
version = "0.1.0"
edition = "2021"
rust-version = "1.97"
description = "Versioned product contracts for Deltamod lifecycle and provider integrations"
license = "EUPL-1.2"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
thiserror = "2"
url = "2"

[dev-dependencies]
tempfile = "3"

[lints.rust]
unsafe_code = "forbid"
