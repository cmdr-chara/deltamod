[package]
name = "deltamod-storage-domain"
version = "0.1.0"
edition = "2021"
description = "Standalone safe storage and profile domain for Deltamod"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
