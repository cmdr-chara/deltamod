{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "IPC and event access for the main Deltamod shell window only.",
  "windows": ["main"],
  "permissions": [
    "core:event:default"
  ]
}
